<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Email OTP Verification | TETRA</title>
  <link rel="stylesheet" href="/styles.css" />
  <style>
    :root {
      --blue-dark: #0a0f2c;
      --blue-darker: #050a1f;
      --accent-blue: #007aff;
      --glass-bg: rgba(255, 255, 255, 0.05);
      --border-color: rgba(255, 255, 255, 0.15);
    }
    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, sans-serif;
      background: linear-gradient(135deg, var(--blue-darker), var(--blue-dark));
      color: white;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }
    .otp-container {
      background: var(--glass-bg);
      border: 1px solid var(--border-color);
      border-radius: 30px;
      padding: 2.5rem 3rem;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.4);
      backdrop-filter: blur(12px);
      max-width: 400px;
      width: 100%;
      text-align: center;
    }
    h1 {
      font-size: 2rem;
      margin-bottom: 1.5rem;
      text-shadow: 0 0 10px rgba(0, 122, 255, 0.2);
    }
    p {
      margin-bottom: 1.5rem;
      font-size: 1rem;
      color: rgba(255, 255, 255, 0.8);
    }
    form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      text-align: left;
    }
    label {
      font-weight: 600;
      font-size: 0.9rem;
      margin-bottom: 0.25rem;
    }
    input {
      padding: 0.75rem 1rem;
      border-radius: 25px;
      border: 1px solid var(--border-color);
      background: rgba(255, 255, 255, 0.1);
      color: white;
      font-size: 1rem;
      outline: none;
      transition: border-color 0.3s ease;
      width: 100%;
      box-sizing: border-box;
      text-align: center;
      letter-spacing: 0.3em;
      font-weight: 600;
    }
    input::placeholder {
      color: rgba(255, 255, 255, 0.6);
      letter-spacing: normal;
      font-weight: normal;
      text-align: left;
    }
    input:focus {
      border-color: var(--accent-blue);
      background: rgba(255, 255, 255, 0.15);
    }
    .btn {
      background-color: var(--accent-blue);
      color: white;
      font-weight: 600;
      padding: 0.75rem 1.5rem;
      border: none;
      border-radius: 25px;
      font-size: 1rem;
      cursor: pointer;
      text-align: center;
      transition: background-color 0.3s ease;
      margin-top: 1rem;
      width: 100%;
    }
    .btn:hover {
      background-color: #005fd9;
    }
    .resend-link {
      margin-top: 1rem;
      font-size: 0.9rem;
      text-align: center;
      color: rgba(255, 255, 255, 0.7);
      cursor: pointer;
      user-select: none;
    }
    .resend-link:hover {
      color: var(--accent-blue);
      text-decoration: underline;
    }
    @media (max-width: 450px) {
      .otp-container {
        padding: 2rem 1.5rem;
      }
    }

    .resend-link.disabled {
  pointer-events: none;
  color: gray;
  opacity: 0.85;
}

/* small spinner used inside the resend button/text */
.spinner-mini {
  width: 14px;
  height: 14px;
  border: 2px solid rgba(255,255,255,0.25);
  border-top-color: white;
  border-radius: 50%;
  display: inline-block;
  vertical-align: middle;
  margin-left: 8px;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

/* keep message fade pleasant */
.error-message {
  transition: opacity 0.35s ease;
}

  </style>
</head>
<body>
  <div class="otp-container">
    <h1>Verify Your Email</h1>
    <div class="error-message" id="errorMessage" style="display:none;"></div>
    <p>Please enter the 6-digit code sent to your email address.</p>
    <form id="otpForm" data-email="<%- email %>">
  <label for="otp">OTP Code</label>
  <input
    type="text"
    id="otp"
    name="otp"
    maxlength="6"
    minlength="6"
    placeholder="Enter the code"
    pattern="\d{6}"
    required
    autocomplete="off"
    inputmode="numeric"
  />
  <button type="submit" class="btn">Verify</button>
</form>
<p class="resend-link" id="resendOtp">Didn't receive the code? Resend</p>
<div class="error-message" id="errorMessage" style="display:none;"></div>

  </div>

  <script src="/js/otp.js"></script>
</body>
</html>
