<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= appName %> | Welcome</title>
  <link rel="stylesheet" href="/styles.css" />
  <style>
    :root {
      --blue-dark: #0a0f2c;
      --blue-darker: #050a1f;
      --accent-blue: #007aff;
      --accent-blue-hover: #005fd9;
      --glass-bg: rgba(255, 255, 255, 0.06);
      --border-color: rgba(255, 255, 255, 0.1);
    }

    body {
      margin: 0;
      font-family: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      background: radial-gradient(circle at top left, var(--blue-dark), var(--blue-darker));
      color: white;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      line-height: 1.6;
    }

    section {
      padding: 80px 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
    }

    /* Hero fills screen on mobile */
    .hero-section {
      min-height: 50vh;
    }

    .card {
      background: var(--glass-bg);
      border: 1px solid var(--border-color);
      border-radius: 24px;
      padding: 2.5rem 2rem;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.4);
      backdrop-filter: blur(14px);
      text-align: center;
      max-width: 650px;
      width: 100%;
      animation: fadeInUp 0.8s ease forwards;

      /* Flex column with spacing */
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }

    

    h1, h2 {
      font-weight: 700;
      letter-spacing: -0.5px;
      text-shadow: 0 0 15px rgba(0, 122, 255, 0.2);
      margin: 0;
    }

    h1 {
      font-size: 2.6rem;
    }

    h2 {
      font-size: 2rem;
    }

    p {
      font-size: 1.1rem;
      color: rgba(255, 255, 255, 0.85);
      margin: 0;
    }

    .btn {
      display: inline-block;
      background: var(--accent-blue);
      color: white;
      font-weight: 600;
      padding: 0.9rem 2rem;
      border-radius: 30px;
      font-size: 1rem;
      cursor: pointer;
      text-decoration: none;
      transition: all 0.3s ease;
      box-shadow: 0 6px 20px rgba(0, 122, 255, 0.4);
    }

    .btn:hover {
      background: var(--accent-blue-hover);
      box-shadow: 0 8px 28px rgba(0, 122, 255, 0.6);
      transform: translateY(-2px);
    }

    footer {
      background: rgba(0, 0, 0, 0.35);
      padding: 2.5rem 1rem;
      text-align: center;
      color: rgba(255, 255, 255, 0.6);
      font-size: 0.9rem;
      margin-top: auto;
    }

    footer p {
      margin: 0.5rem 0;
    }

    footer a {
      color: var(--accent-blue);
      text-decoration: none;
      margin: 0 0.5rem;
      transition: color 0.3s ease;
    }

    footer a:hover {
      color: var(--accent-blue-hover);
      text-decoration: underline;
    }

    /* Animations */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Responsive */
    @media (max-width: 768px) {
      section {
        padding: 60px 16px;
      }
      .card {
        padding: 2rem 1.5rem;
        gap: 1.2rem;
      }
      h1 {
        font-size: 2.1rem;
      }
      h2 {
        font-size: 1.6rem;
      }
      p {
        font-size: 1rem;
      }
    }

    @media (max-width: 480px) {
      section {
        padding: 50px 12px;
      }
      .card {
        padding: 1.8rem 1.25rem;
        gap: 1rem;
      }
      h1 {
        font-size: 1.8rem;
      }
      h2 {
        font-size: 1.3rem;
      }
      p {
        font-size: 0.95rem;
      }
      .btn {
        width: 90%;
        font-size: 1rem;
        padding: 1rem 1.2rem;
      }
    }
  </style>
</head>
<body>

  <!-- Hero Section -->
  <section class="hero-section">
    <div class="card hero">
      <h1>Welcome to <%= appName %></h1>
      <p>Stream, share, and discover your favorite music.</p>
      <a href="/signup" class="btn">Get Started</a>
    </div>
  </section>

  <!-- Description Section -->
  <section>
    <div class="card description">
      <h2>Why TETRA?</h2>
      <p>TETRA is your all-in-one platform for experiencing music like never before. Release singles, albums, and remixes, track your impact on charts, and connect with fans across the world. Whether you're a creator or a listener, this is your stage.</p>
    </div>
  </section>

  <!-- Footer Section -->
  <footer>
    <p>© 2025 Tetra Metropolis, under Dossier Software Development Services</p>
    <p>
      Contact:
      <a href="mailto:contact@tetra.com">contact@tetra.com</a> |
      <a href="https://twitter.com">X (formerly Twitter)</a> |
      <a href="https://instagram.com">Instagram</a>
    </p>
  </footer>

</body>
</html>
