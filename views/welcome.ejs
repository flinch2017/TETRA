<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= appName %> | Welcome</title>
  <link rel="stylesheet" href="/styles.css" />
  <style>
    :root {
      --blue-dark: #0a0f2c;
      --blue-darker: #050a1f;
      --accent-blue: #007aff;
      --accent-blue-hover: #005fd9;
      --glass-bg: rgba(255, 255, 255, 0.06);
      --border-color: rgba(255, 255, 255, 0.1);
    }

    body {
      margin: 0;
      font-family: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      background: radial-gradient(circle at top left, var(--blue-dark), var(--blue-darker));
      color: white;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      line-height: 1.6;
    }

    section {
      padding: 80px 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
    }

    .hero-section {
      min-height: 50vh;
    }

    .cardx {
  background: var(--glass-bg);
  border: 1px solid var(--border-color);
  border-radius: 24px;
  padding: 2.5rem 2rem;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.4);
  backdrop-filter: blur(14px);
  text-align: center;
  max-width: 650px;
  width: 90%;
  animation: fadeInUp 0.8s ease forwards;

  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  position: relative;
  overflow: hidden; /* ensure glow doesn’t leak outside */
}

/* Futuristic animated gradient behind card */
.cardx::before {
  content: "";
  position: absolute;
  inset: -2px; /* slightly bigger than card */
  border-radius: inherit;
  background: linear-gradient(
    270deg,
    #007aff,
    #00ffe7,
    #7b61ff,
    #ff2fd6,
    #007aff
  );
  background-size: 400% 400%;
  animation: gradientFlow 12s ease infinite;
  z-index: -1; /* sits behind content */
  filter: blur(30px);
  opacity: 0.6;
}

/* Animate gradient */
@keyframes gradientFlow {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}


    h1, h2 {
      font-weight: 700;
      letter-spacing: -0.5px;
      text-shadow: 0 0 15px rgba(0, 122, 255, 0.3);
      margin: 0;
    }

    h1 {
      font-size: 2.6rem;
    }

    h2 {
      font-size: 2rem;
    }

    p {
      font-size: 1.1rem;
      color: rgba(255, 255, 255, 0.85);
      margin: 0;
    }

    .btn {
      display: inline-block;
      background: black;
      color: white;
      font-weight: 600;
      padding: 0.9rem 2rem;
      border-radius: 30px;
      font-size: 1rem;
      cursor: pointer;
      text-decoration: none;
      transition: all 0.3s ease;
      box-shadow: 0 8px 28px rgba(0, 122, 255, 0.6);
    }

    .btn:hover {
      background: white;
      color: black;
      box-shadow: 0 8px 28px rgba(0, 122, 255, 0.6);
      transform: translateY(-2px);
    }

    /* Equalizer inside card */
    .equalizer {
      display: flex;
      justify-content: center;
      align-items: flex-end;
      gap: 4px;
      height: 40px;
    }

    .bar {
      width: 6px;
      background: white;
      border-radius: 3px;
      animation: bounce 1s infinite ease-in-out;
    }

    .bar:nth-child(1) { animation-delay: 0s; }
    .bar:nth-child(2) { animation-delay: 0.2s; }
    .bar:nth-child(3) { animation-delay: 0.4s; }
    .bar:nth-child(4) { animation-delay: 0.6s; }
    .bar:nth-child(5) { animation-delay: 0.8s; }

    @keyframes bounce {
      0%, 100% { height: 10px; }
      50% { height: 40px; }
    }

    footer {
      background: rgba(0, 0, 0, 0.35);
      padding: 2.5rem 1rem;
      text-align: center;
      color: rgba(255, 255, 255, 0.6);
      font-size: 0.9rem;
      margin-top: auto;
    }

    footer p {
      margin: 0.5rem 0;
    }

    footer a {
      color: var(--accent-blue);
      text-decoration: none;
      margin: 0 0.5rem;
      transition: color 0.3s ease;
    }

    footer a:hover {
      color: var(--accent-blue-hover);
      text-decoration: underline;
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @media (max-width: 768px) {
      section { padding: 60px 16px; }
      .cardx { padding: 2rem 1.5rem; gap: 1.2rem; margin-top: -40px; }
      h1 { font-size: 2.1rem; }
      h2 { font-size: 1.6rem; }
      p { font-size: 1rem; }
    }

    @media (max-width: 480px) {
      section { padding: 50px 12px; }
      .cardx { padding: 1.8rem 1.25rem; gap: 1rem; margin-top: -40px; }
      h1 { font-size: 1.8rem; }
      h2 { font-size: 1.3rem; }
      p { font-size: 0.95rem; }
      .btn { width: 90%; font-size: 1rem; padding: 1rem 1.2rem; }
    }

    .footer-links {
  margin-top: 1rem;
  display: flex;
  justify-content: center;
  gap: 1rem;
}

.icon-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 42px;
  height: 42px;
  border-radius: 50%;
  background: rgba(255,255,255,0.08);
  color: white;
  font-size: 1.2rem;
  text-decoration: none;
  transition: all 0.3s ease;
  border: 1px solid rgba(255,255,255,0.2);
}

.icon-btn:hover {
  background: linear-gradient(135deg, #00ffe7, #7b61ff);
  color: #fff;
  transform: translateY(-3px) scale(1.1);
  box-shadow: 0 0 12px rgba(123,97,255,0.6), 0 0 20px rgba(0,255,231,0.4);
}

  </style>
</head>
<body>

  <!-- Hero Section -->
  <section class="hero-section">
    <div class="cardx hero">
      <h1>Welcome to <%= appName %></h1>
      <p>Stream, share, and discover your favorite music.</p>
      <div class="equalizer">
        <div class="bar"></div>
        <div class="bar"></div>
        <div class="bar"></div>
        <div class="bar"></div>
        <div class="bar"></div>
      </div>
      <a href="/signup" class="btn">Get Started</a>
    </div>
  </section>

  <!-- Description Section -->
  <section>
    <div class="cardx description">
      <h2>Why TETRA?</h2>
      <p>TETRA is your all-in-one platform for experiencing music like never before. Release singles, albums, and remixes, track your impact on charts, and connect with fans across the world. Whether you're a creator or a listener, this is your stage.</p>
      <div class="equalizer">
        <div class="bar"></div>
        <div class="bar"></div>
        <div class="bar"></div>
        <div class="bar"></div>
        <div class="bar"></div>
      </div>
    </div>
  </section>

  <!-- Footer Section -->
  <footer>
  <p>© 2025 Tetra Metropolis, under Dossier Software Development Services</p>
  <div class="footer-links">
    <a href="mailto:thedossiercreatives@gmail.com" class="icon-btn" title="Email">
      <i class="fas fa-envelope"></i>
    </a>
    <a href="https://twitter.com" class="icon-btn" title="Twitter / X">
      <i class="fab fa-x-twitter"></i>
    </a>
    <a href="https://instagram.com" class="icon-btn" title="Instagram">
      <i class="fab fa-instagram"></i>
    </a>
  </div>
</footer>

</body>
</html>
