<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Pricing Plans</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">

  <!-- Stripe.js -->
  <script src="https://js.stripe.com/v3/"></script>

  <!-- PayPal JS SDK -->
  <script src="https://www.paypal.com/sdk/js?client-id=AZ3vwRr1sqKm0Fm3jQAP8nkMmkCjaYvGxuSYRBaNvfRNOGSdqAJ_xUXKkn8go9a8eS7oegV6lSFkYorj&currency=USD"></script>

  <style>
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, #0f0f0f, #1a1a1a, #131313, #0c0c0c);
      background-size: 300% 300%;
      animation: gradientMove 20s ease infinite;
      z-index: -3;
    }

    body::after {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: url('https://www.transparenttextures.com/patterns/asfalt-dark.png');
      opacity: 0.08;
      z-index: -2;
      pointer-events: none;
    }

    @keyframes gradientMove {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .firefly {
      position: fixed;
      top: 50%;
      left: 50%;
      width: 6px;
      height: 6px;
      background: #facc15;
      border-radius: 50%;
      box-shadow: 0 0 8px 2px #facc15;
      opacity: 0.8;
      animation: fly 20s linear infinite;
      z-index: -1;
    }

    @keyframes fly {
      0% { transform: translate(0, 0) scale(1); opacity: 0.5; }
      25% { transform: translate(-200px, -300px) scale(0.8); opacity: 1; }
      50% { transform: translate(300px, -100px) scale(1.2); opacity: 0.7; }
      75% { transform: translate(-150px, 200px) scale(1); opacity: 1; }
      100% { transform: translate(0, 0) scale(1); opacity: 0.5; }
    }

    .firefly:nth-child(1) { top: 20%; left: 30%; animation-delay: 0s; }
    .firefly:nth-child(2) { top: 60%; left: 70%; animation-delay: 3s; }
    .firefly:nth-child(3) { top: 40%; left: 20%; animation-delay: 6s; }
    .firefly:nth-child(4) { top: 80%; left: 50%; animation-delay: 9s; }
    .firefly:nth-child(5) { top: 30%; left: 80%; animation-delay: 12s; }
    .firefly:nth-child(6) { top: 50%; left: 60%; animation-delay: 15s; }
    .firefly:nth-child(7) { top: 70%; left: 40%; animation-delay: 18s; }
  </style>
</head>

<body class="text-gray-100">

  <!-- Fireflies -->
  <div class="firefly"></div>
  <div class="firefly"></div>
  <div class="firefly"></div>
  <div class="firefly"></div>
  <div class="firefly"></div>
  <div class="firefly"></div>
  <div class="firefly"></div>

  <div class="min-h-screen flex flex-col items-center justify-center py-10 px-4 max-w-7xl mx-auto">
    <h1 class="text-4xl font-bold mb-6 text-white">Choose Your Artist Plan</h1>

    <div class="w-full grid grid-cols-1 lg:grid-cols-3 gap-8">

      <!-- Basic Artist Plan -->
      <div class="bg-gray-900 rounded-2xl shadow-xl p-6 w-full max-w-xs mx-auto text-center">
        <h2 class="text-2xl font-semibold mb-2 text-white">Basic Artist</h2>
        <p class="text-gray-400 mb-4">Perfect for new creators</p>
        <p class="text-4xl font-bold text-blue-400 mb-4">$5/year</p>
        <ul class="mb-6 space-y-2 text-sm text-gray-300">
          <li>🎵 Stream all public music</li>
          <li>📀 Release 3 Singles and 1 EP</li>
          <li>⏱ 48 hours release verification support</li>
        </ul>
        <div id="paypal-button-container-basic"></div>
      </div>

      <!-- Mid Artist Plan -->
      <div class="bg-gray-900 rounded-2xl shadow-xl p-6 w-full max-w-xs mx-auto text-center">
        <h2 class="text-2xl font-semibold mb-2 text-white">Mid Artist</h2>
        <p class="text-gray-400 mb-4">Upload, release & grow</p>
        <p class="text-4xl font-bold text-blue-400 mb-4">$10/year</p>
        <ul class="mb-6 space-y-2 text-sm text-gray-300">
          <li>🎵 Stream all public music</li>
          <li>📀 Release 6 Singles, 2 EPs, and 1 Album</li>
          <li>⏱ 36 hours release verification support</li>
          <li>💬 Socialize/Post to feed</li>
          <li>📻 Tetra Radio/Playlisting Request</li>
        </ul>

        <div id="paypal-button-container-mid" class="mt-4"></div>
      </div>

      <!-- A-Lister Plan -->
      <div class="bg-gray-900 rounded-2xl shadow-xl p-6 w-full max-w-xs mx-auto text-center">
        <h2 class="text-2xl font-semibold mb-2 text-white">A-Lister</h2>
        <p class="text-gray-400 mb-4">For serious artists & pros</p>
        <p class="text-4xl font-bold text-blue-400 mb-4">$25/year</p>
        <ul class="mb-6 space-y-2 text-sm text-gray-300">
          <li>🎵 Stream all public music</li>
          <li>📀 Release 12 Singles, 4 EPs, and 2 Albums</li>
          <li>⏱ 12 hours release verification support</li>
          <li>💬 Socialize/Post to feed</li>
          <li>📻 Tetra Radio/Playlisting Request</li>
          <li>🏆 Exclusive Tetra Title Eligibility</li>
          <li>🌍 Tetra Artist Global Ranking</li>
          <li>🛍 Merch Support</li>
        </ul>
        <div id="paypal-button-container-pro"></div>
      </div>

    </div>
  </div>

  <script>
  ['basic', 'mid', 'pro'].forEach(plan => {
    const containerId = `paypal-button-container-${plan}`;
    if (document.getElementById(containerId)) {
      paypal.Buttons({
        style: {
          layout: 'vertical',
          color: 'blue',
          shape: 'pill',
          label: 'paypal'
        },
        commit: true,

        createOrder: async function (data, actions) {
          try {
            const res = await fetch(`/create-paypal-order?plan=${plan}`, {
              method: 'POST'
            });
            const order = await res.json();
            if (!order.id) throw new Error("Invalid order response");
            return order.id;
          } catch (error) {
            console.error('Create order failed:', error);
            alert('Failed to create PayPal order. Please try again.');
          }
        },

        onApprove: async function (data, actions) {
          try {
            const res = await fetch(`/capture-paypal-order?orderID=${data.orderID}&plan=${plan}`, {
              method: 'POST'
            });
            const result = await res.json();
            window.location.href = '/dashboard';
          } catch (error) {
            console.error('Capture failed:', error);
            alert('Failed to complete PayPal payment. Please try again.');
          }
        },

        onError: function (err) {
          console.error('PayPal error', err);
          alert('There was a problem processing your PayPal payment. Please try again.');
        }
      }).render(`#${containerId}`);
    }
  });
</script>




</body>
</html>
